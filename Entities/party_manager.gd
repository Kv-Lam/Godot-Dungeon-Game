extends Node

var party = [Bard, Mage, Ranger, Rogue] #Characters that the player has obtained.
#var first_character_obtained #This will be whichever character the player picked first. Used to change sprite for player (never got used).
#var unobtained_characters = [Bard, Mage, Ranger, Rogue] #Characters the player has yet to obtain.

#Never got obtaining characters implemented, so this is commented out.
#func obtained_character(character) -> void:
	#var character_index = unobtained_characters.find(character)
	#party.push_back(unobtained_characters[character_index])
	#unobtained_characters.pop_at(character_index)
